import Config from 'react-native-config';

export const API_BASE_URL = Config.API_BASE_URL || 'http://technology.lastminute.com/api';

export const API_ENDPOINTS = {
    HOTELS: 'https://technology.lastminute.com/api/hotel.json',

};
